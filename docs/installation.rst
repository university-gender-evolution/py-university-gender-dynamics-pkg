.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pyugend

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyugend
    $ pip install pyugend
